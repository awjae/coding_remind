import TopNav from '../components/TopNav.js';

export default function Intro({target}) {
  
  this.render = () => {
    const nav = new TopNav({ target, initState: { menu: "intro"} });

  }
  this.render();
}