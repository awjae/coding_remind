import TopNav from '../components/TopNav.js';

export default function Partner({target}) {
  
  this.render = () => {
    const nav = new TopNav({ target, initState: { menu: "partner"} });

  }
  this.render();
}